area_trabajo = ['(x,y,z,)','(a,b,c)','()','etc'] # lista que contiene todos los espacios donde se pueden estampar las letras

def texto():      #Ingresa un texto y lo separa en elementos de una lista.
 caracteres = input("Ingresa Texto: ")
 lista = list(caracteres)
 return lista

# str -> (00,00,00)
# recibe una letra y devuelve las coordenadas donde se guarda esa letra
def coordenadas_home2(letra):                                            #lo mismo que la funcion de arriba pero con un diccionario, + bonito
 Dicc = {'':(algo alto),'h':(xyz),'o':(123),'l':(mno),'a':(987)}

 """if letra == '': # la letra es un espacio     no se si el caso de los espacios debería ir en esta función, creo que no.
   return #no se que devolver """
 if letra in Dicc:
   return Dicc[letra]
 else: print("0h n0! No hemos creado esa letra :C")

 def mover_roboc(lista, area):    #Casos especiales: si el texto es más largo que el area de trabajo
   for i in range(len(lista)):
    letra = lista[i]
    tomar = coordenadas_home2(letra)
    "encender imán" # !! falta la instruccion de encender/ apagar el imán !!
    set.joints(tomar) #tomar la letra
    # ns si vamos a posicionar antes de estampar¿?
    set.joints(area[i])    # estampar la letra
    set.joints(tomar) # lleva la letra de vuelta
    "apagar imán"
